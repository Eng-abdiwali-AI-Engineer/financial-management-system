import React,{useEffect,useState} from 'react';import {Landmark,ArrowUpRight,ArrowDownLeft,RefreshCw,Plus} from 'lucide-react';import {api} from '../main';import Panel from '../components/Panel';
const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n||0);
export default function Banking(){const [d,setD]=useState(null);const load=()=>api('/banking').then(setD);useEffect(load,[]);if(!d)return <div className="p-10 text-center text-sm text-slate-400">Loading banking…</div>;return <div className="mx-auto max-w-[1400px] space-y-5"><div className="flex items-end justify-between"><div><p className="text-xs font-semibold uppercase tracking-wider text-blue-600">Cash & banking</p><h1 className="mt-1 text-2xl font-bold">Banking</h1><p className="mt-1 text-sm text-slate-500">Monitor balances and recent cash movements.</p></div><button onClick={load} className="rounded-lg border bg-white p-2.5 text-slate-500 hover:bg-slate-50"><RefreshCw size={16}/></button></div><div className="grid gap-4 md:grid-cols-3">{d.accounts.map(a=><div key={a.id} className="rounded-xl bg-[#0b2455] p-5 text-white shadow-card"><div className="flex justify-between"><div className="grid h-10 w-10 place-items-center rounded-xl bg-white/10"><Landmark size={19}/></div><span className="text-xs text-blue-200">{a.type}</span></div><p className="mt-7 text-xs text-blue-200">{a.name}</p><p className="mt-1 text-2xl font-bold">{money(a.balance)}</p><p className="mt-2 text-[11px] text-blue-200">•••• {a.last4}</p></div>)}</div><Panel title="Recent banking activity"><div className="space-y-2">{d.activity.map(x=><div key={x.id} className="flex items-center justify-between rounded-xl border border-slate-100 p-4"><div className="flex items-center gap-3"><div className={`grid h-9 w-9 place-items-center rounded-full ${x.direction==='in'?'bg-emerald-50 text-emerald-600':'bg-red-50 text-red-600'}`}>{x.direction==='in'?<ArrowDownLeft size={17}/>:<ArrowUpRight size={17}/>}</div><div><p className="text-sm font-semibold">{x.description}</p><p className="text-[11px] text-slate-400">{x.date} · {x.account}</p></div></div><b className={x.direction==='in'?'text-emerald-600':'text-red-600'}>{x.direction==='in'?'+':'-'}{money(x.amount)}</b></div>)}</div></Panel></div>}
