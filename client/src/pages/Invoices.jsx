import React,{useEffect,useState} from 'react';
import {FileText,Plus,Printer,Receipt,ClipboardList,RefreshCcw,Send,Pencil} from 'lucide-react';
import Panel from '../components/Panel';
const types=[['Invoice','Amounts due from customers',FileText,'blue'],['Quote','Proposals and estimates',ClipboardList,'violet'],['Receipt','Proof of customer payment',Receipt,'emerald'],['Purchase Order','Orders sent to suppliers',Send,'amber'],['Credit Note','Credits and adjustments',RefreshCcw,'rose'],['Payment Request','Formal payment requests',FileText,'slate']];
const initial={type:'Invoice',number:'INV-1003',party:'',email:'',issueDate:new Date().toISOString().slice(0,10),dueDate:new Date().toISOString().slice(0,10),notes:'Thank you for your business.',description:'',quantity:1,rate:''};
export default function Invoices(){const [doc,setDoc]=useState(initial);const [saved,setSaved]=useState(()=>JSON.parse(localStorage.getItem('sfm_documents')||'[]'));const [show,setShow]=useState(false);const [editing,setEditing]=useState(null);const total=(Number(doc.quantity)||0)*(Number(doc.rate)||0);const update=(key,value)=>setDoc({...doc,[key]:value});const openNew=(type='Invoice')=>{setEditing(null);setDoc({...initial,type});setShow(true)};const edit=item=>{setEditing(item.id);setDoc(item);setShow(true)};const create=e=>{e.preventDefault();const record={...doc,total,id:editing||Date.now()};setSaved(editing?saved.map(item=>item.id===editing?record:item):[record,...saved]);setEditing(null);setShow(false)};useEffect(()=>localStorage.setItem('sfm_documents',JSON.stringify(saved)),[saved]);return <div className="mx-auto max-w-[1400px] space-y-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-600">Business documents</p><h1 className="mt-2 text-3xl font-black tracking-tight">Invoices & documents</h1><p className="mt-1 text-sm text-slate-500">Create, organize, edit, and print the paperwork your business needs.</p></div><button onClick={()=>openNew()} className="flex items-center justify-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-xs font-bold text-white shadow-lg shadow-blue-200 hover:bg-blue-700"><Plus size={16}/>Create document</button></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{types.map(([name,description,Icon,tone])=><button key={name} onClick={()=>openNew(name)} className="group rounded-xl border border-slate-100 bg-white p-4 text-left shadow-card transition hover:-translate-y-0.5 hover:border-blue-200 hover:shadow-lg"><div className={`mb-4 grid h-10 w-10 place-items-center rounded-xl ${tone==='blue'?'bg-blue-50 text-blue-600':tone==='violet'?'bg-violet-50 text-violet-600':tone==='emerald'?'bg-emerald-50 text-emerald-600':tone==='amber'?'bg-amber-50 text-amber-600':tone==='rose'?'bg-rose-50 text-rose-600':'bg-slate-100 text-slate-600'}`}><Icon size={19}/></div><p className="text-sm font-bold text-slate-900">{name}</p><p className="mt-1 text-xs text-slate-500">{description}</p></button>)}</div><Panel title="Recent documents" subtitle={`${saved.length} created in this workspace`} action={<button onClick={()=>openNew()} className="flex items-center gap-1 text-xs font-bold text-blue-600"><Plus size={14}/>New</button>}><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead><tr className="border-b text-[11px] uppercase tracking-wider text-slate-400"><th className="pb-3">Document</th><th className="pb-3">Customer / supplier</th><th className="pb-3">Date</th><th className="pb-3 text-right">Amount</th><th className="pb-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{saved.map(item=><tr key={item.id}><td className="py-4"><p className="font-bold">{item.number}</p><span className="text-xs text-slate-400">{item.type}</span></td><td className="py-4 text-slate-600">{item.party||'Unnamed contact'}</td><td className="py-4 text-xs text-slate-500">{item.issueDate}</td><td className="py-4 text-right font-bold">{money(item.total)}</td><td className="py-4 text-right"><button onClick={()=>edit(item)} className="rounded-lg p-2 text-slate-400 hover:bg-blue-50 hover:text-blue-600" title="Edit document"><Pencil size={16}/></button><button onClick={()=>window.print()} className="rounded-lg p-2 text-slate-400 hover:bg-blue-50 hover:text-blue-600" title="Print document"><Printer size={16}/></button></td></tr>)}</tbody></table>{!saved.length&&<div className="py-14 text-center"><FileText className="mx-auto text-slate-300" size={30}/><p className="mt-3 text-sm font-semibold text-slate-600">No documents yet</p><p className="mt-1 text-xs text-slate-400">Start with an invoice, quote, receipt, or purchase order.</p></div>}</div></Panel>{show&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><form onSubmit={create} className="max-h-[calc(100vh-2rem)] w-full max-w-2xl overflow-y-auto rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><h2 className="text-xl font-black">{editing?'Edit':'Create'} business document</h2><p className="mt-1 text-xs text-slate-400">Prepare a professional record for your customer or supplier.</p></div><button type="button" onClick={()=>{setEditing(null);setShow(false)}} className="text-slate-400">✕</button></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><Field label="Document type"><select value={doc.type} onChange={e=>update('type',e.target.value)} className="input">{types.map(([name])=><option key={name}>{name}</option>)}</select></Field><Field label="Document number"><input required value={doc.number} onChange={e=>update('number',e.target.value)} className="input"/></Field><Field label="Customer / supplier"><input required value={doc.party} onChange={e=>update('party',e.target.value)} className="input" placeholder="Business or contact name"/></Field><Field label="Email"><input type="email" value={doc.email} onChange={e=>update('email',e.target.value)} className="input" placeholder="name@company.com"/></Field><Field label="Issue date"><input type="date" required value={doc.issueDate} onChange={e=>update('issueDate',e.target.value)} className="input"/></Field><Field label="Due date"><input type="date" value={doc.dueDate} onChange={e=>update('dueDate',e.target.value)} className="input"/></Field><Field label="Description" wide><input required value={doc.description} onChange={e=>update('description',e.target.value)} className="input" placeholder="Product or service description"/></Field><Field label="Quantity"><input type="number" min="1" required value={doc.quantity} onChange={e=>update('quantity',e.target.value)} className="input"/></Field><Field label="Unit price"><input type="number" min="0" step="0.01" required value={doc.rate} onChange={e=>update('rate',e.target.value)} className="input" placeholder="0.00"/></Field><Field label="Notes" wide><textarea value={doc.notes} onChange={e=>update('notes',e.target.value)} className="input min-h-20"/></Field></div><div className="mt-5 flex items-center justify-between rounded-lg bg-slate-50 p-4"><span className="text-sm font-semibold text-slate-600">Document total</span><span className="text-xl font-black text-slate-900">{money(total)}</span></div><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={()=>{setEditing(null);setShow(false)}} className="rounded-lg border px-4 py-2 text-sm font-semibold">Cancel</button><button type="submit" className="flex items-center gap-2 rounded-lg bg-blue-600 px-5 py-2 text-sm font-bold text-white"><FileText size={15}/>{editing?'Save changes':'Save document'}</button></div></form></div>}</div>}
function Field({label,children,wide}){return <label className={wide?'sm:col-span-2':''}><span className="mb-1.5 block text-xs font-semibold">{label}</span>{children}</label>}
function money(n){return new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n||0)}
