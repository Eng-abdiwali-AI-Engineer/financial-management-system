import React,{useState} from 'react';
import {User,Building2,Bell,ShieldCheck,Save,KeyRound,LockKeyhole,CheckCircle2} from 'lucide-react';
import {api} from '../main';

export default function Settings({user,setUser}){
 const [section,setSection]=useState('profile');
 return <div className="mx-auto max-w-[1100px] space-y-5"><div><p className="text-xs font-semibold uppercase tracking-wider text-blue-600">Workspace controls</p><h1 className="mt-1 text-2xl font-bold">Settings</h1><p className="mt-1 text-sm text-slate-500">Manage your identity, preferences, and account protection.</p></div><div className="grid gap-5 lg:grid-cols-[240px_1fr]"><div className="rounded-xl border border-slate-100 bg-white p-2 shadow-card"><Side icon={User} label="Profile" active={section==='profile'} onClick={()=>setSection('profile')}/><Side icon={Building2} label="Business" active={section==='business'} onClick={()=>setSection('business')}/><Side icon={Bell} label="Notifications" active={section==='notifications'} onClick={()=>setSection('notifications')}/><Side icon={ShieldCheck} label="Security" active={section==='security'} onClick={()=>setSection('security')}/></div>{section==='security'?<Security/>:<Profile section={section} user={user} setUser={setUser}/>}</div></div>
}
function Profile({section,user,setUser}){const [name,setName]=useState(user.name);const [company,setCompany]=useState(user.company||'My Business');const [saved,setSaved]=useState(false);const save=async()=>{try{const d=await api('/auth/profile',{method:'PUT',body:JSON.stringify({name,company})});setUser(d.user);setSaved(true);setTimeout(()=>setSaved(false),1800)}catch(e){alert(e.message)}};return <div className="rounded-xl border border-slate-100 bg-white shadow-card"><div className="border-b p-5"><h2 className="font-bold">{section==='profile'?'Profile & business':section[0].toUpperCase()+section.slice(1)}</h2><p className="mt-1 text-xs text-slate-400">{section==='notifications'?'Notification preferences are ready for your finance alerts.':'Keep your account information up to date.'}</p></div>{section==='notifications'?<div className="space-y-3 p-5"><Toggle label="Payment reminders"/><Toggle label="Monthly financial summary"/><Toggle label="Security alerts"/></div>:<div className="space-y-5 p-5"><label className="block"><span className="mb-2 block text-xs font-semibold">Full name</span><input value={name} onChange={e=>setName(e.target.value)} className="w-full rounded-lg border px-3 py-2.5 text-sm outline-none focus:border-blue-500"/></label><label className="block"><span className="mb-2 block text-xs font-semibold">Email</span><input disabled value={user.email} className="w-full rounded-lg border bg-slate-50 px-3 py-2.5 text-sm text-slate-500"/></label><label className="block"><span className="mb-2 block text-xs font-semibold">Business name</span><input value={company} onChange={e=>setCompany(e.target.value)} className="w-full rounded-lg border px-3 py-2.5 text-sm outline-none focus:border-blue-500"/></label><button onClick={save} className="flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-xs font-bold text-white hover:bg-blue-700"><Save size={15}/>{saved?'Saved':'Save changes'}</button></div>}</div>}
function Security(){const [currentPassword,setCurrent]=useState('');const [newPassword,setNew]=useState('');const [confirm,setConfirm]=useState('');const [message,setMessage]=useState('');const [error,setError]=useState('');const save=async()=>{setMessage('');setError('');if(newPassword!==confirm){setError('New passwords do not match');return}try{await api('/auth/password',{method:'PUT',body:JSON.stringify({currentPassword,newPassword})});setCurrent('');setNew('');setConfirm('');setMessage('Password updated successfully.')}catch(e){setError(e.message)}};return <div className="rounded-xl border border-slate-100 bg-white shadow-card"><div className="border-b p-5"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-50 text-emerald-700"><LockKeyhole size={19}/></div><div><h2 className="font-bold">Security & access</h2><p className="mt-1 text-xs text-slate-400">Use a strong password to protect financial records.</p></div></div></div><div className="space-y-5 p-5"><label className="block"><span className="mb-2 block text-xs font-semibold">Current password</span><input type="password" autoComplete="current-password" value={currentPassword} onChange={e=>setCurrent(e.target.value)} className="w-full rounded-lg border px-3 py-2.5 text-sm outline-none focus:border-blue-500"/></label><label className="block"><span className="mb-2 block text-xs font-semibold">New password</span><input type="password" autoComplete="new-password" minLength="10" value={newPassword} onChange={e=>setNew(e.target.value)} className="w-full rounded-lg border px-3 py-2.5 text-sm outline-none focus:border-blue-500"/><span className="mt-1 block text-[11px] text-slate-400">At least 10 characters.</span></label><label className="block"><span className="mb-2 block text-xs font-semibold">Confirm new password</span><input type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} className="w-full rounded-lg border px-3 py-2.5 text-sm outline-none focus:border-blue-500"/></label>{error&&<p className="text-xs font-semibold text-rose-600">{error}</p>}{message&&<p className="flex items-center gap-2 text-xs font-semibold text-emerald-700"><CheckCircle2 size={15}/>{message}</p>}<button onClick={save} className="flex items-center gap-2 rounded-lg bg-slate-900 px-4 py-2.5 text-xs font-bold text-white hover:bg-slate-700"><KeyRound size={15}/>Update password</button></div></div>}
function Side({icon:Icon,label,active,onClick}){return <button onClick={onClick} className={`flex w-full items-center gap-3 rounded-lg px-3 py-3 text-left text-sm ${active?'bg-blue-50 font-semibold text-blue-700':'text-slate-500 hover:bg-slate-50'}`}><Icon size={17}/>{label}</button>}
function Toggle({label}){const [on,setOn]=useState(true);return <button onClick={()=>setOn(!on)} className="flex w-full items-center justify-between rounded-lg border border-slate-100 p-4 text-left text-sm font-semibold"><span className="flex items-center gap-2"><Bell size={16} className="text-slate-400"/>{label}</span><span className={`h-5 w-9 rounded-full p-0.5 transition ${on?'bg-emerald-500':'bg-slate-200'}`}><span className={`block h-4 w-4 rounded-full bg-white transition ${on?'translate-x-4':''}`}/></span></button>}
